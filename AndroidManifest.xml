<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.custocamera"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="25" />

    <!-- 使用SD卡 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 相機權限相關 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <!-- <uses-permission android:name="com.android.browser.permission.READ_HISTORY_BOOKMARKS" /> -->


    <!-- 震動 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateHidden" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Camera2Page1"
            android:label="@string/title_activity_camera2_page1" >
        </activity>
        <activity
            android:name=".Camera1Page1"
            android:label="@string/title_activity_camera1_page1" >
        </activity>
        <activity
            android:name=".Camera2Page2"
            android:label="@string/title_activity_camera2_page2" >
        </activity>
        <activity
            android:name=".Camera1Page2"
            android:label="@string/title_activity_camera1_page2" >
        </activity>
        <activity
            android:name=".Camera1Page3"
            android:label="@string/title_activity_camera1_page3" >
        </activity>
        <activity
            android:name=".Camera2Page3"
            android:label="@string/title_activity_camera2_page3" >
        </activity>
    </application>

</manifest>
